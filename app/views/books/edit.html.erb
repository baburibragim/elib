<div class="text-2xl font-bold pb-2">
  Редактировать книгу
  "<%= @book.name %>"
</div>

<section class="flex p-6 my-4 bg-green-50">
  <%= render 'form', book: @book, categories: @categories %>
</section>
